import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  ScaleConversionBox: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginTop: 20,
  },
});
